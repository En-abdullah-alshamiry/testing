
⿡ دوال الوقت والتاريخ في PHP

تُستخدم دوال الوقت والتاريخ للتعامل مع التاريخ الحالي، الوقت، وتنسيقهما.

🔹 أهم الدوال:

🔸 date()

تعرض التاريخ أو الوقت الحالي حسب التنسيق.

echo date("Y-m-d"); // 2025-12-17
echo date("H:i:s"); // 22:30:15

🔸 time()

تعطي الطابع الزمني (Timestamp).

echo time();

🔸 strtotime()

تحويل نص إلى وقت.

echo date("Y-m-d", strtotime("next Friday"));

🔸 date_default_timezone_set()

تحديد المنطقة الزمنية.

date_default_timezone_set("Asia/Riyadh");
echo date("H:i:s");


#------------------------------------------------------------------------------

⿢ دوال التعامل مع الملفات في PHP

تُستخدم للتعامل مع الملفات مثل القراءة، الكتابة، والحذف.


---

🔹 فتح ملف

$file = fopen("data.txt", "w");


---

🔹 الكتابة في ملف

fwrite($file, "مرحبا PHP");
fclose($file);


---

🔹 قراءة ملف

$file = fopen("data.txt", "r");
echo fread($file, filesize("data.txt"));
fclose($file);


---

🔹 دوال شائعة أخرى

الدالة	الوظيفة

file_exists()	التحقق من وجود ملف
unlink()	حذف ملف
filesize()	حجم الملف
file_get_contents()	قراءة الملف مباشرة


if (file_exists("data.txt")) {
    echo file_get_contents("data.txt");
}
